# web-scraper-downloader
Web scraping tool: reads URLs from a CSV, extracts title/description/images, downloads assets into a per-item local folder, and generates data.json plus a report.csv with success/error status. Built with an extensible structure to add new websites.
